{
  "name": "icu-assistant-server",
  "version": "1.0.0",
  "description": "ICU Clinical Assistant — Node.js orchestration backend over VNPT SmartBot + SmartVoice",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "fuzzball": "^2.1.3"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
